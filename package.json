{
  "name": "anydown",
  "version": "1.0.0",
  "description": "ANYDOWN — YouTube Video Downloader wrapped as an Android app via Capacitor",
  "private": true,
  "scripts": {
    "build:www": "mkdir -p www && cp templates/index.html www/index.html",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "postinstall": "echo 'Run: npm run build:www && npm run cap:add:android && npm run cap:sync'"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
